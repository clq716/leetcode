- 每道题一个独立 `.java` 文件，类名采用 `$_四位题号_中文标题` 命名，并在类内定义 `Solution` 内部类承载提交方法。
- 提交代码必须被 `//leetcode submit region begin(Prohibit modification and deletion)` 与 `end` 注释对完整包裹，禁止在区间外修改。
- 运行入口统一使用同文件中的 `static void main()`，并通过 `new 外层类().new Solution()` 获取实例调用方法。
- 调试输出一律通过 `import static utils.Printer.print;` 调用 `print(...)`，而非直接 `System.out.println`。