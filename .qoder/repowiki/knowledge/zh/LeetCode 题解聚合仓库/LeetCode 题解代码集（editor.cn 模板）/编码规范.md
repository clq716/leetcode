- 每个题解文件遵循 LeetCode Editor 模板：外层 public class 与文件名一致，内含非 public 的 `Solution` 内部类，并通过 `new ClassName().new Solution()` 在 main 中实例化调用。
- 可提交的方法体严格包裹在 `//leetcode submit region begin(Prohibit modification and deletion)` 与 `//leetcode submit region end(Prohibit modification and deletion)` 注释之间，禁止修改该区域外的框架代码。
- 文件首行以多行注释形式保留从编辑器导出的题目描述、示例、约束与 Related Topics，作为文档与元数据随源码一起版本控制。
- 新题命名优先使用英文 PascalCase（如 `BestTimeToBuyAndSellStock.java`），带题号的旧题采用 `_NNNN_Title.java` 前缀，中文题号则用 `[面试题 xxx]xxx.java` 形式。
- 已废弃的实现迁移到 `oldcode/` 子包，保持主目录仅含活跃题解，避免混淆。